// import { findOrCreateGoogleUser } from "./user.server";

describe("findOrCreateGoogleUser", () => {
  describe("when user doesn't exist", () => {
    it.todo("creates new user");
    it.todo("updates profile info");
  });

  describe("when user exists", () => {
    it.todo("returns existing user");
    it.todo("updates profile info");
  });

  describe("associates users to organization by email domain", () => {
    it.todo("works");
  });
});
