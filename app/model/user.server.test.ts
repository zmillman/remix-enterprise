// import { findOrCreateUserId } from "./user.server";

describe("findOrCreateUserId", () => {
  describe("when user doesn't exist", () => {
    it.todo("creates new user");
    it.todo("updates profile info");
  });

  describe("when user exists", () => {
    it.todo("returns existing user");
    it.todo("updates profile info");
  });
});
